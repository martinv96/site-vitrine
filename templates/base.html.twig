<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>" />

    {% block stylesheets %}
      <link rel="stylesheet" href="{{ asset('styles/app.css') }}" />
    {% endblock %}

    {% block javascripts %}
      {{ encore_entry_script_tags('app') }}
      {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
  </head>
  <body class="bg-gray-50 text-gray-800 font-sans leading-relaxed">

    {# 🔹 NAVBAR FIXE EN HAUT #}
    <nav class="sticky top-0 bg-white shadow-md z-50">
      <div class="max-w-7xl mx-auto px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <a href="{{ path('app_home') }}" class="text-3xl font-extrabold text-blue-700 tracking-wide hover:text-blue-800 transition">
            VitrinePro
          </a>
          <div class="hidden md:flex space-x-10 font-medium text-gray-700">
            <a href="{{ path('app_home') }}" class="hover:text-blue-600 transition">Accueil</a>
            <a href="#services" class="hover:text-blue-600 transition">Services</a>
            <a href="{{ path('about') }}" class="hover:text-blue-600 transition">À propos</a>
            <a href="{{ path('contact') }}" class="text-blue-600 font-semibold hover:underline transition">Contact</a>
          </div>
        </div>
      </div>
    </nav>

    {# 🔸 Contenu dynamique #}
    {% block body %}{% endblock %}

  </body>
</html>
